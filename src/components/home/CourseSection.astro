---
import Section from "../ui/Section.astro";
import Button from "../ui/Button.astro";
import { getCheckoutUrl } from "../../lib/checkout";

const checkout = getCheckoutUrl({ content: "course_section", medium: "section" });
---

<Section id="course" title="The course (hosted on the platform)">
  <div class="grid grid--2">
    <div class="card">
      <h3 class="h3">What you get</h3>
      <ul class="bullets">
        <li>Structured lessons + routine (placeholder)</li>
        <li>Technique cues and “common mistakes”</li>
        <li>Progress path (beginner → consistent habits)</li>
        <li>Access via course platform after purchase</li>
      </ul>
    </div>

    <div class="card join">
      <h3 class="h3">Join now</h3>
      <p class="muted">
        Checkout and access are handled by the platform. This website is the learning + trust hub.
      </p>

      <div class="join__cta">
        <Button
          href={checkout}
          variant="primary"
          target="_blank"
          rel="noreferrer"
          data-ff-event="checkout_click"
          data-ff-content="course_section"
          data-ff-outbound="checkout"
        >
          Go to checkout
        </Button>

        <Button
          href="#free-tips"
          variant="secondary"
          data-ff-event="scroll_click"
          data-ff-content="course_to_free_tips"
        >
          Get free tips
        </Button>
      </div>

      <p class="join__note">Placeholder pricing: $100+ • Start anytime</p>
    </div>
  </div>

  <style>
    .join__cta { margin-top: 12px; display: flex; gap: 12px; flex-wrap: wrap; align-items: center; }
    .join__note { margin: 12px 0 0; color: rgba(238, 240, 255, 0.62); font-size: 12px; }
  </style>
</Section>
