---
import Section from "../ui/Section.astro";
---

<Section id="results" title="Results & social proof">
  <div class="grid grid--2">
    <div class="card">
      <h3 class="h3">Before / After</h3>
      <p class="muted">Placeholder — add real examples later.</p>

      <div class="ba">
        <div class="ph">Before</div>
        <div class="ph">After</div>
        <div class="ph">Before</div>
        <div class="ph">After</div>
      </div>
    </div>

    <div class="card">
      <h3 class="h3">What women say</h3>

      <div class="quotes">
        <blockquote>
          “After a few weeks my face looked more rested and open.”
          <cite>— Student A</cite>
        </blockquote>
        <blockquote>
          “The routine is simple — I can actually stick to it.”
          <cite>— Student B</cite>
        </blockquote>
        <blockquote>
          “I didn’t realize how much tongue posture affects everything.”
          <cite>— Student C</cite>
        </blockquote>
      </div>
    </div>
  </div>

  <style>
    .ba{ display:grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-top: 14px; }
    .ph{
      height: 120px;
      border-radius: 16px;
      border: 1px dashed rgba(17,17,17,0.22);
      background: rgba(255,255,255,0.55);
      display:grid;
      place-items:center;
      color: rgba(17,17,17,0.55);
      letter-spacing: 0.12em;
      text-transform: uppercase;
      font-size: 12px;
    }

    .quotes{ display:grid; gap: 12px; margin-top: 14px; }
    blockquote{
      margin: 0;
      padding: 14px;
      border-radius: 16px;
      border: 1px solid var(--line-2);
      background: rgba(255,255,255,0.55);
      color: rgba(17,17,17,0.82);
    }
    cite{
      display:block;
      margin-top: 8px;
      font-style: normal;
      color: rgba(17,17,17,0.58);
      font-size: 13px;
    }
  </style>
</Section>
