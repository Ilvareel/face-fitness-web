---
const { id = "", title = "" } = Astro.props;
---
<section id={id} class="section">
  <div class="container">
    {title ? <h2 class="section__title">{title}</h2> : null}
    <slot />
  </div>
</section>
